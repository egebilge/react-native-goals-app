import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  item: {
    padding: 10,
    borderBottomWidth: 1,
    borderBottomColor: "#ccc",
  },

  text: {
    fontSize: 18,
    textAlign: "center",
  },
});

export { styles };
